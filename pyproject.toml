[project]
name = "resume-tailoring-skill"
version = "0.1.0"
description = "Dependencies for the repository-local Codex resume tailoring skill"
requires-python = ">=3.12"
dependencies = [
    "pypdf>=6.14.2",
]

[tool.uv]
package = false
